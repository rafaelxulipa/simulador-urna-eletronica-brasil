/**
 * The real urna holds CONFIRMA disabled for a short window after a valid
 * candidate (or blank vote) is shown, forcing the voter to actually look at
 * the review screen before confirming. See docs/research.md.
 */
export const CONFIRM_LOCK_MS = 1000
